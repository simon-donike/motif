# Keyword arguments of CosineAnnealingWarmupRestarts
# Leave first_cycle_steps unset so epoch-based training inherits trainer.max_epochs.
warmup_steps: 1
first_cycle_steps: null
cycle_mult: 1.0
max_lr: 3e-4
min_lr: 1e-6
gamma: 1.0
